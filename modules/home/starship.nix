{
  programs.starship = {
    enable = true;
    enableZshIntegration = true;
    settings = {
      add_newline = true;
      # username = {
      #   show_always = true;
      #   format = "[$user]($style)@";
      # };
      # directory = {
      #   read_only = "🔒";
      #   truncation_symbol = "…/";
      # };
    };
  };
}
